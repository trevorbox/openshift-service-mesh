apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: istio-cni-node-monitor
  namespace: istio-cni
spec:
  podMetricsEndpoints:
    - interval: 30s
      path: /metrics
      port: metrics
  selector:
    matchLabels:
      k8s-app: istio-cni-node
