apiVersion: config.openshift.io/v1
kind: Authentication
metadata:
  name: cluster
spec:
  oauthMetadata:
    name: ''
  # serviceAccountIssuer: 'https://oidcissuer43658698.blob.core.windows.net/$web/'
  serviceAccountIssuer: ''
  type: ''
  webhookTokenAuthenticator:
    kubeConfig:
      name: webhook-authentication-integrated-oauth
