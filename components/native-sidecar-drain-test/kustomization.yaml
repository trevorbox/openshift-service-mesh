apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: native-sidecar-drain

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: drain-app-server
    files:
      - server.py

resources:
  - deployments.yaml
  - services.yaml
