kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: httpbin-gateway-allow-external
  namespace: httpbin
spec:
  policyTypes:
  - Ingress
  podSelector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: httpbin-gateway
  ingress:
    - {}
