kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: httpbin-allow-external
  namespace: default
spec:
  policyTypes:
  - Ingress
  podSelector:
    matchLabels:
      app: httpbin
  ingress:
    - {}
