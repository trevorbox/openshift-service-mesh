apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: spring-boot-demo-disable-mtls-jfr-port-only
  namespace: spring-boot-demo
spec:
  selector:
    matchLabels:
      app: spring-boot-demo
  mtls:
    mode: STRICT
  portLevelMtls:
    9091:
      mode: DISABLE
