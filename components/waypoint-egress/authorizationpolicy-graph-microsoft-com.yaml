

apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: graph-microsoft-com
spec:
  targetRefs:
  - kind: ServiceEntry
    group: networking.istio.io
    name: graph-microsoft-com
  action: ALLOW
  rules:
  # Allow only certain apps access
  - from:
    - source:
        principals: ["cluster.local/ns/auth2-custom/sa/oauth2-proxy"]
  # - from:
  #   - source:
  #       principals: ["cluster.local/ns/sample/sa/curl"]
